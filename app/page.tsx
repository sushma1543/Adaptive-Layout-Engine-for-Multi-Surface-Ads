'use client';

import AdaptiveLayoutLab from '@/src/App';

/** The shell stays deliberately thin: the app lives in src/ beside the pure engine. */
export default function Home() {
  return <AdaptiveLayoutLab />;
}
